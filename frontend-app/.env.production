# Same-origin monolith: Django serves SPA + /api on one host. Vite reads this at build time.
# For split hosting (e.g. Vercel + separate API), override with full URL in the host env, e.g. https://api.example.com/api
VITE_API_URL=/api
